# -----------------------------
# Python / FastAPI project
# -----------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pkl
*.so
*.egg
*.egg-info/
*.log
*.out
*.tmp
*.bak
*.old

# Virtual environments
.venv/
venv/
env/
ENV/
.python-version
pip-wheel-metadata/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
htmlcov/
.coverage
.coverage.*
nosetests.xml
coverage.xml
*.cover
*.py,cover

# Jupyter
.ipynb_checkpoints/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Hugging Face / models (avoid committing big weights)
.huggingface/
transformers_cache/
~/.cache/huggingface/
*.safetensors
*.bin
*.pt
*.h5

# Node / frontend (if expanded)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/
build/

# Docker & runtime bits
*.pid
*.seed
*.pid.lock

# -----------------------------
# Secrets & local config
# -----------------------------
# Dotenv secrets (keep out of git)
.env
.env.*
!.env.example

# Hide pem files (if any)
*.pem
*.key
*.crt

# If you keep a local-only config override, ignore it:
config.local.toml

# (We DO track config.toml by default so you have versioned defaults.)
# If you ever add secrets to config.toml, switch to:
#   - commit config.example.toml
#   - add `config.toml` here to ignore
