# Ensure consistent line endings
* text=auto eol=lf

# Windows-specific: if you want CRLF locally but LF in repo
# * text eol=crlf

# Treat these as text, enforce LF
*.py   text eol=lf
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.md   text eol=lf
*.toml text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
*.gitignore text eol=lf
*.gitattributes text eol=lf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.gz binary
*.tar binary
*.tgz binary
*.pkl binary
*.pt binary
*.h5 binary

# Large model weights (prevent diffs, optional if you commit them)
*.bin filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text

# Mark Hugging Face / cache files as binary (if ever committed)
*.safetensors binary

# GitHub linguist overrides
# Mark generated files as not source code
*.min.js linguist-generated=true
*.min.css linguist-generated=true

# Documentation tweaks
*.md linguist-documentation
